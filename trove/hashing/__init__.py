"""Content hashing: a cheap partial-read prefilter plus full SHA-256 (``hasher.py``).

Perceptual/near-duplicate hashing lives in ``dedup/``, not here -- this package
only does exact-content hashing.
"""
