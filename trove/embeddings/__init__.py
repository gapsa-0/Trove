"""Local multimodal embeddings for semantic search (SigLIP 2).

The vector space the ``semantic_embeddings`` table holds. Nothing here talks to
the network except the one-time model download in ``backend.ensure_models``.
"""
