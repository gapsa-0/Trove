"""Geographic place clustering (``clusters.py``): groups geolocated files into
durable, user-editable places. Reading GPS off a file's metadata happens in
``metadata/``; this package only clusters coordinates that are already resolved.
"""
