"""The local GUI server: request handling (``server.py``), the route tables
(``routes/``), static asset resolution (``assets.py``), app-icon generation
(``icons.py``) and the standalone-window launcher (``launcher.py``).

Binds to localhost only; see ``server.py`` for why file access has no
path-traversal surface.
"""
