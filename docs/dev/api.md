# API Reference

Generated by `tools/dev/gen_api_docs.py` from the three route tables in
`trove/web/routes/__init__.py` -- `GET_ROUTES`, `GET_PREFIX_ROUTES`,
`POST_ROUTES` -- which are already this server's single source of truth for
what it answers. Do not hand-edit this file; regenerate it with:

    .venv/bin/python tools/dev/gen_api_docs.py

or `make api-docs`. CI runs the same script in `--check` mode and fails the
build if this file has drifted from the route tables.

**71 routes**: 31 GET (exact) + 12 GET (prefix) + 28 POST.

## GET -- exact path

| Path | Description | Handler |
| --- | --- | --- |
| `/api/health` | Liveness plus the build this process is running, which is what the desktop shell polls for before showing a window. | `trove/web/routes/static.py::health` |
| `/api/archives` | Every registered archive, for the picker. | `trove/web/routes/archives.py::archive_list` |
| `/api/features` | Every feature an archive can be given, for the setup panel. | `trove/web/routes/archives.py::feature_list` |
| `/api/settings` | No user-configurable settings exist yet; always answers with an empty object. | `trove/web/routes/static.py::settings` |
| `/api/docs` | Every reference page in reading order, for the index rail. | `trove/web/routes/docs.py::catalogue` |
| `/api/docs/page` | One reference page, rendered, by ``?slug=``. | `trove/web/routes/docs.py::page` |
| `/api/pipeline` | The resolved status of every pipeline stage for one archive. | `trove/web/routes/pipeline.py::snapshot` |
| `/api/summary` | Counts, size, media types and date range for one archive. | `trove/web/routes/overview.py::summary` |
| `/api/timeline` | Counts of matching, non-hidden media bucketed by month or year, for the timeline chart. | `trove/web/routes/overview.py::timeline` |
| `/api/dates/sources` | How many files' dates came from Takeout JSON vs EXIF vs filename vs mtime vs none. | `trove/web/routes/overview.py::date_sources` |
| `/api/dups/summary` | Unique-file, duplicate-group, pending and reclaimable-byte counts, broken down by match type and media type. | `trove/web/routes/dups.py::summary` |
| `/api/dups` | Duplicate groups, largest reclaimable bytes first, with each member's role. | `trove/web/routes/dups.py::groups` |
| `/api/media` | The media grid: filtered, sorted and paginated files for the archive. | `trove/web/routes/browse.py::media` |
| `/api/browse/filters` | The distinct filter values (years, types, folders...) the Browse UI offers. | `trove/web/routes/browse.py::filters` |
| `/api/folders` | The folder tree with per-folder file counts. | `trove/web/routes/browse.py::folders` |
| `/api/browse/semantic/status` | Semantic index state, and whether this archive can search by description. | `trove/web/routes/search.py::semantic_status` |
| `/api/browse/semantic/search` | Free-text semantic search over the archive's media, ranked by embedding similarity. | `trove/web/routes/search.py::semantic_search` |
| `/api/map/clusters` | Place clusters (grouped geotagged files) with at least the configured minimum media. | `trove/web/routes/places.py::clusters` |
| `/api/map/points` | Every geotagged file as a single un-clustered map point. | `trove/web/routes/places.py::points` |
| `/api/map/cluster/merge-preview` | How spread out a prospective cluster merge would be, so the GUI can warn before it's confirmed. | `trove/web/routes/places.py::merge_preview` |
| `/api/faces/summary` | Face/person totals for the Faces overview. | `trove/web/routes/people.py::summary` |
| `/api/faces/persons` | Paginated list of person clusters. | `trove/web/routes/people.py::persons` |
| `/api/faces/suggestions` | The 'same person?' review queue: candidate cluster pairs the automatic pass left apart. | `trove/web/routes/people.py::suggestions` |
| `/api/pets/summary` | Pet/species totals for the Pets overview. | `trove/web/routes/pets.py::summary` |
| `/api/pets` | Paginated list of pet-identity groups. | `trove/web/routes/pets.py::groups` |
| `/api/pet/detections` | Paginated gallery of animal detections, optionally limited to those with no pet assigned. | `trove/web/routes/pets.py::detections` |
| `/api/nonhuman` | The non-human review queue: detections not yet confirmed as animal vs. restored to People. | `trove/web/routes/pets.py::nonhuman` |
| `/` | The single-page app shell HTML. | `trove/web/routes/static.py::index` |
| `/index.html` | The single-page app shell HTML. | `trove/web/routes/static.py::index` |
| `/manifest.webmanifest` | The PWA manifest describing the installable app. | `trove/web/routes/static.py::manifest` |
| `/sw.js` | The service worker script that lets the app be installed and work offline. | `trove/web/routes/static.py::service_worker` |

## GET -- prefix

Ordered: exact paths are matched first, then the first matching prefix here
wins. This table keeps that declared order verbatim -- it is load-bearing,
see the module docstring in `routes/__init__.py`.

| Path | Description | Handler |
| --- | --- | --- |
| `/api/item/` | One file's full detail page, by id. | `trove/web/routes/browse.py::item` |
| `/api/map/cluster/` | One place cluster's member files, paginated. | `trove/web/routes/places.py::cluster_members` |
| `/api/faces/person/` | One person's detail page: their faces, paginated. | `trove/web/routes/people.py::person` |
| `/api/pet/` | One pet's detail page: their detections, paginated. | `trove/web/routes/pets.py::group` |
| `/icon-` | The app icon PNG, 512px or 192px depending on which the request path names. | `trove/web/routes/static.py::icon` |
| `/static/` | One of the app's own stylesheets or scripts, by ``css/<name>`` or ``js/<name>``. | `trove/web/routes/static.py::app_asset` |
| `/vendor/` | A vendored static asset by filename, or 404 if it isn't under the vendor directory. | `trove/web/routes/static.py::vendor` |
| `/archivethumb/` | A thumbnail scoped to a named archive, for the start-page cover mosaic where nothing is 'open' yet. | `trove/web/routes/media.py::archive_thumb` |
| `/thumb/` | A file's thumbnail, generated and cached on first request. | `trove/web/routes/media.py::thumb` |
| `/faceThumb/` | A cropped face thumbnail, cut from the source photo or (for a video detection) its re-derived keyframe. | `trove/web/routes/media.py::face_thumb` |
| `/animalThumb/` | A cropped pet/animal thumbnail, cut from the source photo or (for a video detection) its re-derived keyframe. | `trove/web/routes/media.py::animal_thumb` |
| `/file/` | The original file, or an upright re-encode for a photo stored sideways. | `trove/web/routes/media.py::original` |

## POST

| Path | Description | Handler |
| --- | --- | --- |
| `/api/archives` | Register a new archive by folder path, preparing its private database. | `trove/web/routes/archives.py::add` |
| `/api/archive/configure` | Rename an archive, or change which features it runs. | `trove/web/routes/archives.py::configure` |
| `/api/archive/open` | Open one registered archive so the GUI starts serving its content. | `trove/web/routes/archives.py::open_archive` |
| `/api/archive/close` | Close the currently open archive, stopping its background jobs. | `trove/web/routes/archives.py::close` |
| `/api/archive/remove` | Forget an archive: delete its private database and cache wholesale. | `trove/web/routes/archives.py::remove` |
| `/api/pipeline/pause` | Pause or resume the whole pipeline, or a single stage's card if `stage` is given. | `trove/web/routes/pipeline.py::pause` |
| `/api/faces/person/rename` | Rename a person cluster. | `trove/web/routes/people.py::rename_person` |
| `/api/faces/reassign` | Move one face onto a named person and pin it there so re-clustering keeps it. | `trove/web/routes/people.py::reassign` |
| `/api/faces/merge` | Merge two person clusters the user confirmed are the same, immediately and durably. | `trove/web/routes/people.py::merge` |
| `/api/faces/unmerge` | Undo a person merge and, if needed, kick off a recluster. | `trove/web/routes/people.py::unmerge` |
| `/api/faces/detach` | Release every face of one file from a person and durably block them from drifting back. | `trove/web/routes/people.py::detach` |
| `/api/faces/different` | Record that two person clusters are confirmed NOT the same, so auto-merge never proposes them again. | `trove/web/routes/people.py::mark_different` |
| `/api/faces/skip` | Record that a 'same person?' pair was reviewed and left undecided, so it drops out of the suggestions queue. | `trove/web/routes/people.py::skip` |
| `/api/faces/hide` | Mark a cluster as not a person (animal/toy/cartoon/false detection) and drop it. | `trove/web/routes/people.py::hide` |
| `/api/pet/rename` | Rename a pet-identity group. | `trove/web/routes/pets.py::rename_pet` |
| `/api/pets/merge` | Merge two pet groups the user confirmed are the same animal, immediately and durably. | `trove/web/routes/pets.py::merge` |
| `/api/pets/unmerge` | Undo a pet merge and, if needed, kick off a recluster. | `trove/web/routes/pets.py::unmerge` |
| `/api/nonhuman/review` | Confirm a non-human detection, or restore it to People as unassigned. | `trove/web/routes/pets.py::review_nonhuman` |
| `/api/map/cluster/rename` | Rename a place cluster. | `trove/web/routes/places.py::rename_cluster` |
| `/api/map/cluster/merge` | Merge two place clusters the user confirmed are the same location, immediately. | `trove/web/routes/places.py::merge_clusters` |
| `/api/map/cluster/unmerge` | Undo a place-cluster merge, restoring the dropped cluster verbatim. | `trove/web/routes/places.py::unmerge_clusters` |
| `/api/item/date` | Set one file's date by hand, as a manual override. | `trove/web/routes/browse.py::set_date` |
| `/api/item/place` | Attach or clear one file's place, depending on whether `clear` is set. | `trove/web/routes/places.py::set_item_place` |
| `/api/item/person/add` | Tag a file with a named person by hand, for media where no face was detected. | `trove/web/routes/people.py::add_person` |
| `/api/item/person/remove` | Remove a hand-added person tag from a file. | `trove/web/routes/people.py::remove_person` |
| `/api/item/pet/add` | Tag a file with a named pet by hand. | `trove/web/routes/pets.py::add_pet` |
| `/api/item/pet/remove` | Remove a hand-added pet tag from a file. | `trove/web/routes/pets.py::remove_pet` |
| `/api/places/create` | Create a user-pinned place at a dropped coordinate, optionally attaching a file to it. | `trove/web/routes/places.py::create_place` |
