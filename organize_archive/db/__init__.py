"""SQLite access layer: connection helpers, schema init/migration and the
scan/dedup coverage bookkeeping in ``database.py``.

Foundation (L0) package -- see ``tests/unit/test_layering.py``: nothing here
imports from ``scan/``, ``metadata/``, ``dedup/`` or any other domain package.
"""
